{
  "name": "antoinebackend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "backend": "json-server --watch element.json --routes routes.json --port 5001",
    "build": "json-server --host 0.0.0.0 element.json --port 8083",
      "start": "node server.js",
  "vercel-build": "npm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.3",
    "nodemon": "^2.0.22"
  },
  "devDependencies": {
    "pm2": "^5.3.0",
    "serve": "^14.2.0"
  }
}
